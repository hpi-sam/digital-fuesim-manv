{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",
        "types": ["jest"],
        // These are needed for using it as a library
        "declaration": true,
        "declarationMap": true
    },
    "references": [],
    "include": ["src/**/*"],
    "exclude": ["node_modules"]
}

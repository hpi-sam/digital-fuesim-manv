version: '3'
services:
    digital-fuesim-manv:
        image: digital-fuesim-manv
        build:
            context: .
            dockerfile: docker/Dockerfile
        restart: unless-stopped
        container_name: digital-fuesim-manv
        ports:
            - 80:80
